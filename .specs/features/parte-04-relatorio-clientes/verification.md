# Parte 04 - Relatório de clientes verification

**Verdict**: PASS
**Profile**: ui
**Diff range**: 512d806..300fc39
**Round**: 2 - scoped
**Verifier**: independent sub-agent (author != verifier)

Rodada 1 foi feita em `7f98519` e devolveu FAIL por três gaps (mensagens do controlador em UTF-8 duplamente codificado e provas de mensagem tautológicas). O fix `300fc39` foi verificado por conta própria, não tomado como dado:

- `git diff 7f98519..300fc39` toca exatamente `src/Aplicacao/Aplicacao.ControladorRelatorioCliente.pas`, `tests/Unitarios/Testes.FiltroRelatorioCliente.pas`, `tests/Unitarios/Testes.ControladorRelatorioCliente.pas`, `tests/Unitarios/Testes.FormFiltroRelatorioCliente.pas` e artefatos `.specs` (relatório da rodada 1 e lições). Nenhum outro fonte de produção mudou.
- Codificação conferida nos bytes: `Aplicacao.ControladorRelatorioCliente.pas` começa em `EF BB BF` e a linha 55 traz `4E C3A3 6F` (`Não`), não mais `4E C383 C2A3 6F`. A varredura `grep -rlP '\xc3\x83\xc2' src tests --include=*.pas --include=*.dfm --include=*.dpr` não retorna nenhum arquivo.
- As quatro constantes de mensagem e as quatro mensagens de `EArgumentNilException` foram corrigidas; nenhuma outra linha desse fonte mudou.
- Nos três fontes de teste, cada `Assert.AreEqual(MENSAGEM_*, ...)` virou o literal do check, inline na asserção ou na construção do caso da tabela; as substituições são 1:1 e não deslocaram linhas.

Escopo desta rodada, conforme "Re-verifying after a fix": provas re-executadas **em cheio** no novo `HEAD`; citações dos três fontes de teste tocados refrescadas; faltas re-injetadas nas superfícies tocadas e na superfície nova criada pelo fix; linha de `Coverage` cuja autoridade o fix tocou recomputada; linhas de `Test policy` que classificam arquivo tocado re-julgadas. Passo 1 carrega de `7f98519` porque o fix não tocou a interface.

Builds em `300fc39` antes das provas: `tmp\build-testes.cmd` exit 0 (`49131 lines ... 8299452 bytes code`) e `tmp\build-app.cmd` exit 0 (`4697 lines ... 6427032 bytes code`); nenhum `CadCli.exe` estava em execução, então o `F2039` avisado não ocorreu. Os 27 testes nomeados numa única invocação: `Tests Found: 27 / Passed: 27 / Failed: 0`, exit 0 — cada um dos 27 filtros nomeados casou exatamente um teste.

## Binding sources

*carried from 7f98519* - o fix não tocou a interface nem o documento, então a enumeração por tela e o veredito de contradição seguem valendo.

| Source | Opened | Contradiction | Uncovered |
| --- | --- | --- | --- |
| `Teste Programador Delphi 2026.md` (Item 1.3, Item 3) | sim - lido em `.specs/Teste Programador Delphi 2026.md` (o link relativo do plano resolve para esse caminho) | nenhuma - os 3 filtros (`Id Inicial e Id Final`, `Cidade/Estado`, `Todos`) e as 7 colunas (`ID, NOME, CPF ou CNPJ, CEP, BAIRRO, CIDADE, ESTADO`) batem com C1/C3/C7/C9 e com C19/C20; ReportBuilder e DevExpress exigidos e provados por C10/C9/C19-C22 | - |
| ReportBuilder Developer's Guide (PDF, 470 páginas) | sim - baixado de digital-metaphors.com e extraído localmente; p.127 `DeviceType := dtReportTextFile` + `TextFileName` + `ShowPrintDialog := False`, p.85 `DeviceType` default `Screen` e `ModalPreview` default `True`, p.221 `TppReportTextFileDevice` | nenhuma - o adaptador (`Infraestrutura.GeradorRelatorioClienteReportBuilder.pas:268-288`) segue exatamente esse contrato; o reinício exigido pelo AC 13 é satisfeito por construir um `TppReport` novo a cada `Visualizar` | - |

Enumeração por tela (o que a fonte vinculante e o `Landing`/`Observable` do plano decidem e um seletor alcança):

- screen `FiltroRelatorioCliente` - **controles presentes (11, contagem asserida)**: `GrupoModos`, `RotuloIdInicial`, `EditorIdInicial`, `RotuloIdFinal`, `EditorIdFinal`, `RotuloEstado`, `ComboEstado`, `RotuloCidade`, `ComboCidade`, `BotaoVisualizar`, `BotaoFechar` -> C9 (`Assert.AreEqual(11, LVerificados, 'A tela tem exatamente os 11 controles do contrato.')`). **Ausentes**: a contagem exata de 11 fecha o conjunto, então nenhum controle extra pode existir.
- screen - **textos**: caption, legenda do grupo, 3 itens do grupo na ordem do enunciado, 4 rótulos, 2 botões -> C9; `Todas as cidades` -> C4/C7.
- screen - **ordem e contagem**: 3 modos (`Assert.AreEqual(3, ...Items.Count)`), 4 estados, 4 cidades -> C9/C7.
- screen - **arranjo/composição**: uma única região (`GrupoModos`) contendo os 3 modos, e fora dela quatro editores e dois botões empilhados; verticalidade grupo -> IDs -> estado/cidade -> botões e `TabOrder` crescente -> C9 (três asserções `Top + Height <=` e seis `TabOrder <`). Não há exemption em branco: espaçamento, cor e peso de fonte não são asseridos e não são decididos por nenhuma fonte vinculante (o enunciado só exige "componentes visuais DevExpress", provado por C9 via `ClassType.UnitName` começando em `cx`/`dx`).
- screen - **estados**: empty (modo Todos, campos desabilitados) C2/C7; loading (`Visualizar` desabilitado durante a geração) C15/C23; error (vazio e falha com mensagens distintas) C16/C17/C18; unauthorised e confirmação destrutiva são `n/a` no plano.
- document `Relatório de Clientes` - **estrutura**: título, `Emitido em ...`, filtro aplicado, linha de cabeçalho com as 7 colunas na ordem, linhas em ID crescente, `Página k de N` -> C19/C20.

Observação (não é finding): o enunciado exige "utilizar o enter como tab" dentro de *Item 2 - Requisitos obrigatórios da rotina de cadastro de clientes*; o relatório é Item 3 e o plano não estende a regra, então a ausência de `KeyPreview` na tela de filtros não é contradição com a fonte.

## Checks

*Provas verified at 300fc39 (re-executadas em cheio). Citações dos três fontes de teste tocados pelo fix - `Testes.FiltroRelatorioCliente.pas`, `Testes.ControladorRelatorioCliente.pas`, `Testes.FormFiltroRelatorioCliente.pas` - refrescadas em `300fc39`; as demais citações carried from 7f98519.*

| Check | Claim | Proof run | Evidence | Result |
| --- | --- | --- | --- | --- |
| C1 | validação table-driven sobre 20 entradas | `CadCli.Testes.exe --run:TTestesFiltroRelatorioCliente.ValidaENormalizaAsVinteEntradasDaTabela` (lote de 27 em `300fc39`, exit 0) | `tests/Unitarios/Testes.FiltroRelatorioCliente.pas:110` - `Assert.AreEqual(LCaso.Valido, LResultado.Valido, LRotulo)`; `:122` - `Assert.AreEqual(Ord(LCaso.Campo), Ord(LResultado.Campo), LRotulo + ' - campo indicado')`; `:123` - `Assert.AreEqual(LCaso.Mensagem, LResultado.Mensagem, LRotulo + ' - mensagem')` com os literais do check na tabela (`:75` - `Mensagem: 'O ID inicial deve ser menor ou igual ao ID final.'`; `:78`/`:85` - `Mensagem: 'Selecione um estado.'`); `:127` - `Assert.AreEqual(20, LVerificados, ...)`; tabela em `:40-92` | PASS |
| C2 | `Iniciar` abre em Todos, desabilita tudo, `ListarEstados` 1x, 4 estados | idem | `tests/Unitarios/Testes.ControladorRelatorioCliente.pas:104` - `Assert.AreEqual(Ord(mrTodos), Ord(FVisaoObjeto.ModosExibidos[0]), ...)`; `:107` - `Assert.AreEqual(1, FRepositorioObjeto.ChamadasEstados, ...)`; `:109` - `Assert.AreEqual('Bahia,Minas Gerais,Rio de Janeiro,São Paulo', FVisaoObjeto.NomesDosEstados)`; `:110-111` - `AreEqual(0, ...ChamadasRelatorio)` e `AreEqual(0, FGeradorObjeto.Chamadas)` | PASS |
| C3 | cada modo habilita somente seus campos | idem | `tests/Unitarios/Testes.ControladorRelatorioCliente.pas:117-128` - `Assert.IsTrue(FVisaoObjeto.IntervaloHabilitado, ...)` / `Assert.IsFalse(FVisaoObjeto.CidadeEstadoHabilitado, ...)` nos três modos + `Assert.AreEqual(3, FVisaoObjeto.HabilitacoesIntervalo, ...)` | PASS |
| C4 | `SelecionarEstado(SP)` chama `ListarCidades` 1x e entrega 4 itens; `0` não consulta | idem | `tests/Unitarios/Testes.ControladorRelatorioCliente.pas:134` - `Assert.AreEqual(1, FRepositorioObjeto.ChamadasCidades, ...)`; `:137` - `Assert.AreEqual('Todas as cidades,Campinas,Santos,São Paulo', FVisaoObjeto.NomesDasCidades)`; `:141-143` - `AreEqual(1, ...ChamadasCidades)` e `AreEqual('Todas as cidades', ...)` | PASS |
| C5 | filtro inválido foca o 1º campo, avisa 1x com a mensagem de C1, não consulta | idem | `tests/Unitarios/Testes.ControladorRelatorioCliente.pas:189` - `Assert.AreEqual(Ord(LCaso.Campo), Ord(FVisaoObjeto.UltimoCampoFocado), ...)`; `:192` - `Assert.AreEqual(LCaso.Mensagem, FVisaoObjeto.Avisos[0])` com os literais em `:163` (`'Informe um ID inicial inteiro maior que zero.'`) a `:175` (`'Selecione um estado.'`); `:193` - `Assert.AreEqual(-1, FVisaoObjeto.Registro.IndexOf('Carregamento:True'), ...)`; `:198-199` - `AreEqual(0, ...ChamadasRelatorio)` e `AreEqual(0, FGeradorObjeto.Chamadas)` | PASS |
| C6 | falha em `ListarEstados` exibe `Não foi possível carregar os estados e cidades.` e mantém Todos utilizável | idem | `tests/Unitarios/Testes.ControladorRelatorioCliente.pas:210` - `Assert.AreEqual('Não foi possível carregar os estados e cidades.', FVisaoObjeto.Erros[0])` (literal, não mais a constante); `:209` - `AreEqual(1, FVisaoObjeto.Erros.Count, ...)`; `:211` - `Assert.IsFalse(ContainsText(FVisaoObjeto.Erros[0], 'SELECT'), ...)`; `:218` - `AreEqual(1, FRepositorioObjeto.ChamadasRelatorio, ...)`. Valor de produção conferido nos bytes em `src/Aplicacao/Aplicacao.ControladorRelatorioCliente.pas:55` (`4E C3A3 6F`) | PASS |
| C7 | form real: modos habilitam campos, estado filtra cidades | idem | `tests/Unitarios/Testes.FormFiltroRelatorioCliente.pas:136` - `Assert.AreEqual(Ord(mrTodos), FForm.GrupoModos.ItemIndex, ...)`; `:137-140`, `:144-161` - `Assert.IsFalse/IsTrue(FForm.EditorIdInicial.Enabled, ...)` etc.; `:164` - `Assert.AreEqual('Bahia,Minas Gerais,Rio de Janeiro,São Paulo', string.Join(...))`; `:174` - `Assert.AreEqual('Todas as cidades,Campinas,Santos,São Paulo', LNomes)` | PASS |
| C8 | form real: inválido deixa `ActiveControl` no editor certo | idem | `tests/Unitarios/Testes.FormFiltroRelatorioCliente.pas:188` - `Assert.IsTrue(FocoEstaEm(FForm, FForm.EditorIdInicial), ...)`; `:192` - `AreEqual(1, FApresentadorObjeto.Mensagens.Count, ...)`; `:193` - `Assert.AreEqual('Informe um ID inicial inteiro maior que zero.', FApresentadorObjeto.Mensagens[0])`; `:194` - `AreEqual(0, FGeradorObjeto.Chamadas, ...)`; `:201` - `Assert.IsTrue(FocoEstaEm(FForm, FForm.ComboEstado), ...)`; `:205` - `Assert.AreEqual('Selecione um estado.', FApresentadorObjeto.Mensagens[1])` | PASS |
| C9 | textos, ordem, arranjo, `TabOrder` e DevExpress | idem | `tests/Unitarios/Testes.FormFiltroRelatorioCliente.pas:217` - `Assert.AreEqual('Relatório de Clientes', FForm.Caption)`; `:220-222` - itens do grupo na ordem; `:229` - `Assert.AreEqual(Integer(mrCancel), Integer(FForm.BotaoFechar.ModalResult), ...)`; `:232-237` - `Assert.IsTrue(FForm.GrupoModos.Top + FForm.GrupoModos.Height <= FForm.EditorIdInicial.Top, ...)` e as outras duas; `:239-249` - seis `TabOrder <`; `:255` - `Assert.IsTrue(StartsText('cx', LComponente.ClassType.UnitName) or StartsText('dx', ...))`; `:261` - `Assert.AreEqual(11, LVerificados, ...)` | PASS |
| C10 | controlador e domínio sem Vcl/FireDAC/cx/dx/pp; ReportBuilder só no adaptador | idem | `tests/Unitarios/Testes.FormFiltroRelatorioCliente.pas:371-373` - `AssegurarSemPrefixos('Aplicacao.ControladorRelatorioCliente', PROIBIDOS_CONTROLADOR)` (+ domínio e form); `:377` - `Assert.IsTrue(ContainsStr(..., 'TFormFiltroRelatorioCliente = class(TForm, IVisaoRelatorioCliente)'), ...)`; `:385` - `Assert.AreEqual(1, LQuantidade, ...)`; `:400` - `Assert.IsFalse(ContainsStr(LTexto, LUnitRb), LUnitRb + ' só pode aparecer no adaptador: ' + LArquivo)` | PASS |
| C11 | intervalo inclui os dois limites (4 casos na F5) | idem (Firebird `localhost:3050`) | `tests/Unitarios/Testes.RepositorioClienteFirebird.pas:670` - `Assert.AreEqual(LCaso.Esperados, IdsDoRelatorio(FRepositorio.ListarParaRelatorio(FiltroIntervalo(LCaso.De, LCaso.Ate))), ...)`; tabela `2..4->'2,3,4'`, `1..1->'1'`, `4..9->'4,5'`, `6..9->''` em `:658-661`; `:675` - `Assert.AreEqual(4, LVerificados, 'Os quatro intervalos devem ser asseridos.')` | PASS |
| C12 | cidade/estado: 6 combinações | idem | `tests/Unitarios/Testes.RepositorioClienteFirebird.pas:706` - `Assert.AreEqual(LCaso.Esperados, IdsDoRelatorio(FRepositorio.ListarParaRelatorio(FiltroCidadeEstado(EstadoId(LCaso.Uf), LCidadeId))), ...)`; tabela das 6 combinações em `:687-692`; `:711` - `Assert.AreEqual(6, LVerificados, 'As seis combinações devem ser asseridas.')` | PASS |
| C13 | Todos: 60 clientes em ID crescente + F5 com cidade/UF/estado | idem | `tests/Unitarios/Testes.RepositorioClienteFirebird.pas:726` - `Assert.AreEqual(60, Integer(Length(LClientes)), 'O relatório não corta os clientes.')`; `:734` - `Assert.AreEqual(LEsperados, IdsDoRelatorio(LClientes), 'Os IDs vêm em ordem crescente.')`; `:739-746` - `AreEqual('1,2,3,4,5', IdsDoRelatorio(LClientes))`, `AreEqual('Centro', LClientes[2].Bairro)`, `AreEqual('Campinas', ...Cidade)`, `AreEqual('SP', ...Uf)`, `AreEqual('São Paulo', ...Estado)`, `AreEqual('', LClientes[4].Cidade/Uf/Estado)` | PASS |
| C14 | `ListarEstados` e `ListarCidades` por nome | idem | `tests/Unitarios/Testes.RepositorioClienteFirebird.pas:753` - `Assert.AreEqual('BA - Bahia,MG - Minas Gerais,RJ - Rio de Janeiro,SP - São Paulo', NomesDosEstados(FRepositorio.ListarEstados), ...)`; `:755` - `AreEqual('Belo Horizonte,Contagem,Mariana,Uberlândia', ...ListarCidades(EstadoId('MG')))`; `:757` - `AreEqual('Campinas,Santos,São Paulo', ...ListarCidades(EstadoId('SP')))` | PASS |
| C15 | consulta 1x, entrega 3 clientes na ordem, emissão do relógio, 4 descrições, carregamento | idem | `tests/Unitarios/Testes.ControladorRelatorioCliente.pas:275` - `Assert.AreEqual(1, FRepositorioObjeto.ChamadasRelatorio, ...)`; `:288` - `Assert.AreEqual('8,1,5', FGeradorObjeto.IdsRecebidos, ...)`; `:290` - `Assert.AreEqual(LCasos[I].Descricao, FGeradorObjeto.Dados.DescricaoFiltro)` com as 4 descrições literais em `:247/:250/:255/:260`; `:291` - `Assert.AreEqual('22/09/2026 14:30', FormatDateTime('dd/mm/yyyy hh:nn', FGeradorObjeto.Dados.Emissao), ...)`; `:294-299` - ordem `Carregamento:True < ListarParaRelatorio` e `Gerar < Carregamento:False`; `:302` - `AreEqual(4, LVerificados, ...)` | PASS |
| C16 | 0 clientes: avisa sem abrir pré-visualização | idem | `tests/Unitarios/Testes.ControladorRelatorioCliente.pas:315` - `Assert.AreEqual(0, FGeradorObjeto.Chamadas, 'Sem resultado não abre pré-visualização.')`; `:317` - `Assert.AreEqual('Nenhum cliente encontrado para o filtro informado', FVisaoObjeto.Avisos[0])` (literal); `:318-321` - `IsFalse(...Carregando)`, `AreEqual(0, ...Fechamentos)`, `AreEqual(0, Length(...ModosExibidos))` | PASS |
| C17 | falha na consulta e no gerador: mensagens fixas, carregamento por último, retentativa | idem | `tests/Unitarios/Testes.ControladorRelatorioCliente.pas:342` - `Assert.AreEqual('Não foi possível consultar os clientes do relatório.', FVisaoObjeto.Erros[0])`; `:359` - `Assert.AreEqual('Não foi possível gerar o relatório de clientes.', FVisaoObjeto.Erros[0])` (ambos literais); `:340` - `AreEqual(0, FGeradorObjeto.Chamadas, ...)`; `:343`/`:360` - `IsFalse(ContainsText(..., 'Firebird'/'TppReport'))`; `:345` e `:362` - `AreEqual(FVisaoObjeto.Registro.Count - 1, FVisaoObjeto.Registro.IndexOf('Carregamento:False'), 'O carregamento encerra por último.')`; `:365`/`:367` - modo e habilitações preservados; `:376-381` - segunda tentativa gera 1x com o filtro preservado. Valores de produção conferidos em `src/Aplicacao/Aplicacao.ControladorRelatorioCliente.pas:56-57` | PASS |
| C18 | form real: falha na geração preserva filtro e reabilita `Visualizar` | idem | `tests/Unitarios/Testes.FormFiltroRelatorioCliente.pas:276-279` - `AreEqual(Ord(mrIntervalo), FForm.GrupoModos.ItemIndex, ...)`, `AreEqual('2', FForm.EditorIdInicial.Text, ...)`, `AreEqual('4', FForm.EditorIdFinal.Text, ...)`, `Assert.IsTrue(FForm.BotaoVisualizar.Enabled, ...)`; `:280` - `AreEqual(1, FApresentadorObjeto.Mensagens.Count, ...)`; `:281` - `Assert.AreEqual('Não foi possível gerar o relatório de clientes.', FApresentadorObjeto.Mensagens[0])` (literal) | PASS |
| C19 | página única: título, emissão, filtro, paginação, 7 colunas em ordem, linhas 1/3/5 | idem | `tests/Unitarios/Testes.GeradorRelatorioCliente.pas:149-153` - `Assert.IsTrue(ContainsStr(LTexto, 'Relatório de Clientes'), ...)`, `'Emitido em 22/09/2026 14:30'`, `'Filtro: Todos'`, `'Página 1 de 1'`; `:154` - `Assert.AreEqual(1, LinhasDeCabecalho(LLinhas), ...)` com o regex `'ID\s+NOME\s+CPF/CNPJ\s+CEP\s+BAIRRO\s+CIDADE\s+ESTADO'` em `:107-108`; `:158-160` - ordem 1 < 3 < 5; `:164-170` - `Carlos Silva`, `11.222.333/0001-81`, `13010-000`, `Centro`, `Campinas`, `SP`; `:174-175` - cliente 5 sem cidade nem UF | PASS |
| C20 | 120 clientes: N>=2 páginas numeradas, cabeçalho repetido, cada ID 1x em ordem | idem | `tests/Unitarios/Testes.GeradorRelatorioCliente.pas:199` - `Assert.IsTrue(LTotal >= 2, ...)` sobre `LGeradorObjeto.TotalDePaginas` (= `AbsolutePageCount`, `src/Infraestrutura/Infraestrutura.GeradorRelatorioClienteReportBuilder.pas:288`); `:203` - `Assert.IsTrue(ContainsStr(LTexto, Format('Página %d de %d', [I, LTotal])), ...)`; `:205` - `Assert.AreEqual(LTotal, LinhasDeCabecalho(LLinhas), ...)`; `:211` - `Assert.AreEqual(1, OcorrenciasDoId(LLinhas, I), ...)`; `:213` - `Assert.IsTrue(IndiceDaLinhaDoId(LLinhas, I) > LAnterior, ...)` | PASS |
| C21 | segunda visualização reinicia o relatório | idem | `tests/Unitarios/Testes.GeradorRelatorioCliente.pas:235-237` - `Assert.IsTrue(ContainsStr(LTexto, 'Filtro: Estado MG'), ...)`, `Assert.IsFalse(ContainsStr(LTexto, 'Filtro: Todos'), ...)`, `Assert.IsTrue(ContainsStr(LTexto, 'Página 1 de 1'), ...)`; `:238-242` - `AreEqual(1, OcorrenciasDoId(LLinhas, 2))`, `(4)`, e `AreEqual(0, ...)` para 1, 3 e 5 | PASS |
| C22 | destino de produção: 1 `TppPrintPreview` modal, liberada, sem diálogo de impressão | idem | `tests/Unitarios/Testes.GeradorRelatorioCliente.pas:300-303` - `Assert.AreEqual(1, LFechador.PreVisualizacoes, ...)`, `Assert.AreEqual(1, LFechador.Modais, 'A pré-visualização é modal.')`, `Assert.AreEqual(0, LFechador.Dialogos, ...)`; `:308` - `Assert.AreEqual(0, LRemanescentes, ...)`; `:309` - `Assert.AreEqual(LFormsAntes, Screen.FormCount, ...)` | PASS |
| C23 | form real: `Visualizar` desabilitado durante a geração | idem | `tests/Unitarios/Testes.FormFiltroRelatorioCliente.pas:299-301` - `Assert.AreEqual(1, FGeradorObjeto.Chamadas, ...)`, `Assert.IsFalse(LHabilitadoDurante, 'Visualizar fica desabilitado durante a geração.')`, `Assert.IsTrue(FForm.BotaoVisualizar.Enabled, ...)`; captura em `:290-294` | PASS |
| C24 | `ComporNavegador(...).AbrirRelatorio` exibe 1 form modal em Todos com 4 estados e a libera | idem (Firebird) | `tests/Unitarios/Testes.NavegadorAplicacao.pas:345` - `Assert.AreEqual(1, FRelatoriosExibidos, 'Deve ser exibida exatamente 1 TFormFiltroRelatorioCliente.')`; `:347` - `Assert.IsTrue(FRelatorioModal, ...)`; `:348` - `Assert.AreEqual(2, FModoNoRelatorio, 'A tela abre com o modo Todos selecionado.')`; `:349` - `Assert.AreEqual(4, FEstadosNoRelatorio, 'O combo de estado traz os 4 estados da base.')`; `:352` - `Assert.AreEqual(LFormsAntes, Screen.FormCount, 'A tela deve ser liberada ao fechar.')`. Assembly de produção lido diretamente: `src/Visao/Visao.ComposicaoAplicacao.pas:84-88` - `LNavegador.RegistrarTelaRelatorio(function: TForm begin Result := CriarTelaRelatorio(AConexao); end)` | PASS |
| C25 | `AD-017` active em `STATE.md` + prova da Parte 02 C23 reescrita | comando PowerShell de `checks.md` (`Select-String` em `.specs/STATE.md` pelo padrão da linha `AD-017` com status `active`), exit 0; runner idem | `.specs/STATE.md:23` - linha de tabela com `AD-017` e status `active` em `2026-09-22`; `tests/Unitarios/Testes.NavegadorAplicacao.pas:238` - `LNavegador := TNavegadorAplicacao.Create(FFormPrincipal)` (sem telas registradas); `:244` - `end, ENavegacaoSemTela)`; `:245` e `:257` - `Assert.AreEqual(LFormsAntes, Screen.FormCount, 'Nenhuma form pode ser criada.')`; `:256` - `Assert.AreEqual('Não foi possível abrir o relatório de clientes.', LVisaoObjeto.Erros[0])` | PASS |
| C26 | `CadCli.dproj` sem pacote `rb*`/`pp*`; entrega da Parte 02 verde | comando PowerShell de `checks.md` (`Select-String` em `CadCli.dproj` pelo padrão `DCC_UsePackage` seguido de token `rb`/`pp`, invertendo o exit code), exit 0; runner idem (após `tmp\build-app.cmd` Release exit 0 em `300fc39`) | `tests/Unitarios/Testes.EntregaRunner.pas:219` - `Assert.AreEqual(LFechamento.Count, LPresentes.Count, 'As BPLs presentes devem ser exatamente o fechamento importado.')`; `:213` e `:216` - as duas inclusões mútuas; `:200` - `Assert.AreEqual(0, Integer(Length(TDirectory.GetFiles(LDiretorio, 'CadCli*.bpl'))), ...)` | PASS |
| C27 | contratos de `IRepositorioCliente` e `IGeradorRelatorioCliente`; SQL termina em `ORDER BY C.ID` sem `FIRST` | idem | `tests/Unitarios/Testes.FormFiltroRelatorioCliente.pas:414` - `Assert.IsTrue(ContainsStr(..., 'function ListarParaRelatorio(const AFiltro: TFiltroRelatorioCliente): TClientes;'), ...)`; `:417`/`:419` - `ListarEstados: TEstados` e `ListarCidades(AEstadoId: Integer): TCidades`; `:424` - `'procedure Visualizar(const ADados: TDadosRelatorioCliente);'`; `:427` - `Assert.AreEqual(1, Integer(TRegEx.Matches(LTexto, <regex de procedure ou function seguida de abre-parêntese>).Count), 'IGeradorRelatorioCliente declara exatamente um método.')`; `:436` - `Assert.IsTrue(ContainsStr(LSql, '''ORDER BY C.ID'''), ...)`; `:438` - `Assert.IsFalse(ContainsText(LSql, 'FIRST'), ...)` | PASS |

## Coverage

*A linha `textos da screen (20)` foi recomputada verified at 300fc39, porque o fix tocou a autoridade dela (os valores das mensagens) e as provas que a sustentam. As demais linhas são carried from 7f98519 - o fix não tocou nenhuma das suas autoridades.*

| Set (size) | Recomputed from | Member -> proof | Unproven |
| --- | --- | --- | --- |
| textos da screen (20) - *verified at 300fc39* | plano + `Landing` + valores efetivos das constantes de mensagem, lidos nos bytes de `Dominio.FiltroRelatorioCliente.pas:48-51` e `Aplicacao.ControladorRelatorioCliente.pas:53-57` | `Relatório de Clientes` C9 · `Filtro` C9 · `ID Inicial e ID Final` C9 · `Cidade/Estado` C9 · `Todos` C9 · `ID Inicial` C9 · `ID Final` C9 · `Estado` C9 · `Cidade` C9 · `Todas as cidades` C4/C7 · `Visualizar` C9 · `Fechar` C9 · `Informe um ID inicial inteiro maior que zero.` C1/C5/C8 · `Informe um ID final inteiro maior que zero.` C1/C5 · `O ID inicial deve ser menor ou igual ao ID final.` C1/C5 · `Selecione um estado.` C1/C5/C8 · `Nenhum cliente encontrado para o filtro informado` C16 · `Não foi possível carregar os estados e cidades.` C6 · `Não foi possível consultar os clientes do relatório.` C17 · `Não foi possível gerar o relatório de clientes.` C17/C18 - os 20 agora têm prova que carrega o literal do contrato, não a constante de produção | - |
| modos de filtro (3) - *carried from 7f98519* | enunciado Item 3 (`Id Inicial e Id Final`, `Cidade/Estado`, `Todos`) | intervalo C1/C3/C7/C11/C15 · cidade/estado C1/C3/C7/C12/C15 · todos C1/C2/C3/C7/C13/C15 | - |
| colunas do relatório (7) - *carried from 7f98519* | enunciado Item 3 (`ID, NOME, CPF ou CNPJ, CEP, BAIRRO, CIDADE, ESTADO`) | as 7 numa única linha de cabeçalho, na ordem, C19 (`Testes.GeradorRelatorioCliente.pas:107-108`) e repetida por página C20 | - |
| rejeições da validação (9) - *carried from 7f98519* | plano AC 2/AC 3/AC 5 | vazio · zero · negativo · não numérico · decimal · acima de `MaxInt` · inicial > final · estado ausente · cidade sem estado -> C1 linhas 4-9, 13, 14, 17 | - |
| campo indicado no inválido (3) - *carried from 7f98519* | `TCampoFiltroRelatorio` (`Dominio.FiltroRelatorioCliente.pas:11`) | `cfIdInicial` C1/C5/C8 · `cfIdFinal` C1/C5 · `cfEstado` C1/C5/C8 | - |
| normalização de resíduos (3) - *carried from 7f98519* | plano AC 4 | Todos ignora tudo C1 linha 18 · intervalo ignora estado/cidade C1 linha 19 · cidade/estado ignora IDs C1 linha 20 | - |
| limites do intervalo (4) - *carried from 7f98519* | plano AC 6 | inicial incluso · final incluso · inicial = final · faixa vazia -> C11 (4 linhas asseridas) | - |
| cidade/estado (4) - *carried from 7f98519* | plano AC 7/AC 8 | combinação com e sem clientes · estado com e sem clientes -> C12 (6 linhas asseridas) | - |
| Todos (3) - *carried from 7f98519* | plano AC 9 + door 3 | sem limite C13 · ordem por ID independente da inserção C13 · cliente sem cidade C13/C19 | - |
| desfechos de `Visualizar` (5) - *carried from 7f98519* | `TControladorRelatorioCliente.Visualizar` (`Aplicacao.ControladorRelatorioCliente.pas:197-222`) | sucesso C15/C23 · vazio C16 · inválido C5/C8 · falha na consulta C17 · falha no gerador C17/C18 | - |
| descrição do filtro (4) - *carried from 7f98519* | plano AC 10 + `Descrever` (`:145-158`) | Todos C15/C19 · intervalo C15 · estado C15/C21 · cidade C15 | - |
| elementos do AC 10 (6) - *carried from 7f98519* | plano AC 10 | título · emissão · filtro · paginação · 7 colunas · ID crescente -> C19/C20/C13 | - |
| geração repetida (2) - *carried from 7f98519* | plano AC 13 + RB guide | reinício entre filtros C21 · mesma instância não mistura dados C21 | - |
| destinos do gerador (2) - *carried from 7f98519* | `TDestinoRelatorio` (`Infraestrutura.GeradorRelatorioClienteReportBuilder.pas:27`) | `drPreVisualizacao` C22 · `drArquivoTexto` C19/C20/C21 | - |
| estados do `Observable` da screen (4 aplicáveis) - *carried from 7f98519* | plano `Observable` (7 linhas, 3 `n/a`) | empty C2/C7 · loading C15/C23 · error C16/C17/C18 · density and ordering C9 | - |
| document `Relatório de Clientes` - estrutura (1) - *carried from 7f98519* | plano `Observable` + enunciado | cabeçalho, filtro, tabela, paginação C19/C20 | - |
| arranjo da screen (4) - *carried from 7f98519* | plano `Observable` (density and ordering) | modos acima dos IDs · IDs acima de estado/cidade · estado/cidade acima dos botões · ordem de tabulação -> C9 (`Testes.FormFiltroRelatorioCliente.pas:232-249`) | - |
| one-way doors do `Landing` (3) - *carried from 7f98519* | plano `Landing` | par form/controlador C7/C10 · isolamento do ReportBuilder C10/C27 · contrato visual C13/C19/C20/C27 | - |
| startup config (2 assemblies) - *carried from 7f98519* | leitura direta de cada assembly | produção `Visao.ComposicaoAplicacao.pas:84-88` -> C24 · testes `Testes.NavegadorAplicacao.pas:238` (`TNavegadorAplicacao.Create` sem registro) -> C25 | - |
| estados e cidades da base de referência (4 + 4 + 3) - *carried from 7f98519* | AD-005 / `Migracao.V002.DadosReferencia.pas` (conjunto citado na prosa de `checks.md` sem linha própria) | Bahia/MG/RJ/SP C14/C2/C7 · cidades de MG (4) C14 · cidades de SP (3) C14/C4/C7 | - |

`Relations` e `Surface` são `None` no plano - nenhuma entidade nem rota exige join. `unauthorised` e `destructive action confirms` são `n/a` no `Observable`.

`Swept` resolvidos contra o código (*carried from 7f98519*, com as citações de mensagem refrescadas): validation (C1/C5/C8), failure modes (C16/C17/C18), idempotency (C21), concurrency (C15/C23 - `SinalizarCarregamento` em `Aplicacao.ControladorRelatorioCliente.pas:209/220` e `Visao.FormFiltroRelatorioCliente.pas:208-215`), dependency failure (C6/C17 - `try..except` em `:89-94`, `:164-173`, `:185-194`), state transitions (C3/C4/C7 - `SelecionarEstado` reinicia a cidade em `:120-122`), observability (C6/C17 - `IsFalse(ContainsText(..., 'SELECT'/'Firebird'/'TppReport'))` em `Testes.ControladorRelatorioCliente.pas:211/343/360`). `authorization` e `data lifecycle` são `n/a` (política aprovada).

O **precision gap** apontado na rodada 1 - checks que nomeiam um texto literal provados por `Assert.AreEqual(CONSTANTE, ...)` - está fechado: as seis provas afetadas passaram a carregar o literal, e a falta 8 abaixo demonstra que a asserção agora mata exatamente a regressão de codificação da rodada 1.

## Test policy rows

*As três linhas que classificam arquivos tocados pelo fix foram re-julgadas verified at 300fc39; as outras duas são carried from 7f98519.*

| Row | Files it classifies | Required proof | Expectation met |
| --- | --- | --- | --- |
| Regra de domínio pura (`TValidacaoFiltroRelatorio`) - *verified at 300fc39* | `src/Dominio/Dominio.FiltroRelatorioCliente.pas`; prova em `Testes.FiltroRelatorioCliente.pas` (tocada) | uma prova unitária table-driven -> C1 (`Testes.FiltroRelatorioCliente.pas:94-128`) | yes - 20 linhas asseridas, cobrindo as 9 rejeições, os 3 campos indicados e as 3 normalizações, agora com o literal da mensagem em cada linha de rejeição |
| Controlador que decide sem cruzar fronteira (`TControladorRelatorioCliente`) - *verified at 300fc39* | `src/Aplicacao/Aplicacao.ControladorRelatorioCliente.pas` (tocado); prova em `Testes.ControladorRelatorioCliente.pas` (tocada) | uma prova unitária por desfecho com visão/repositório/gerador/relógio falsos -> C2-C6, C15-C17 | yes - cada modo, os 5 desfechos de `Visualizar`, as 4 descrições e a falha de carga têm prova própria, e as quatro mensagens fixas agora são asseridas pelo texto do contrato |
| View passiva (`TFormFiltroRelatorioCliente`) - *verified at 300fc39* | `src/Visao/Visao.FormFiltroRelatorioCliente.pas` (+ `.dfm`); prova em `Testes.FormFiltroRelatorioCliente.pas` (tocada) | uma prova com form real, controlador real e dependências falsas -> C7, C8, C9, C18, C23 | yes - ligação de cada modo, do estado, de `Visualizar`, textos e arranjo; os três textos apresentados pela form são asseridos por literal |
| Adaptador de persistência (`ListarParaRelatorio`, `ListarEstados`, `ListarCidades`) - *carried from 7f98519* | `src/Infraestrutura/Infraestrutura.RepositorioClienteFireDAC.pas` | integração em base temporária no Firebird local -> C11, C12, C13, C14 | yes - um caso por modo, pelos dois limites do intervalo e pelas combinações com e sem resultado |
| Adaptador do componente comercial (`TGeradorRelatorioClienteReportBuilder`) - *carried from 7f98519* | `src/Infraestrutura/Infraestrutura.GeradorRelatorioClienteReportBuilder.pas` | dispositivo de texto + pré-visualização real -> C19/C20/C21 e C22 | yes - cada elemento do AC 10, paginação com N>=2 e reinício |

## Faults injected

*verified at 300fc39.* Isolamento: `git worktree add C:\Projetos\Jean\CadCli-verify-scratch2 HEAD --detach`. Baseline da árvore real: `git status --porcelain` vazio antes; vazio depois de `git worktree remove --force` + `git worktree prune`, `HEAD` = `300fc39`. Nenhum `git stash` foi usado. Re-injeção escopada às superfícies que o fix tocou (os valores das mensagens) e à superfície que ele criou (as asserções por literal), que nunca havia sido morta uma vez.

| Mutation | Location | Killed |
| --- | --- | --- |
| `MENSAGEM_FALHA_LOCALIDADES` -> `'Falha ao carregar estados e cidades.'` (texto trocado) | `src/Aplicacao/Aplicacao.ControladorRelatorioCliente.pas:55` | yes - C6 `Tests Failed: 1` |
| `MENSAGEM_FALHA_CONSULTA` -> `'Erro ao consultar os clientes do relatorio.'` (texto trocado) | `src/Aplicacao/Aplicacao.ControladorRelatorioCliente.pas:56` | yes - C17 `Tests Failed: 1` |
| `MENSAGEM_FALHA_GERACAO` -> a forma duplamente codificada `'NÃ£o foi possÃ­vel gerar o relatÃ³rio de clientes.'` (reintrodução exata do defeito da rodada 1) | `src/Aplicacao/Aplicacao.ControladorRelatorioCliente.pas:57` | yes - C18 `Tests Failed: 1` |
| `MENSAGEM_ID_INICIAL` -> `'Informe um ID inicial valido.'` (texto trocado) | `src/Dominio/Dominio.FiltroRelatorioCliente.pas:48` | yes - C1 `Tests Failed: 1` |

Quatro faltas, quatro superfícies de asserção distintas (mensagem de carga no controlador, mensagem de consulta no controlador, mensagem de geração na form real, mensagem de validação no domínio), teto de cinco respeitado. A terceira é a prova direta de que a regressão que a rodada 1 encontrou por leitura de bytes agora morre numa asserção.

*carried from 7f98519* - as cinco faltas comportamentais da rodada 1, em superfícies que o fix não tocou: bound deslocado em `Dominio.FiltroRelatorioCliente.pas:88` (C1), condição invertida em `Aplicacao.ControladorRelatorioCliente.pas:102` (C3), limite inferior exclusivo em `Infraestrutura.RepositorioClienteFireDAC.pas:346` (C11), ordem de colunas em `Infraestrutura.GeradorRelatorioClienteReportBuilder.pas:59` (C19) e efeito colateral removido em `Visao.FormFiltroRelatorioCliente.pas:210` (C23) - todas mortas.

## Gate

`.\tests\bin\Win64\Debug\CadCli.Testes.exe --run:<27 testes>` em `300fc39` - 27 passed, 0 failed (exit 0). Provas PowerShell de C25 e C26 - exit 0 e exit 0. Builds prévios: `tmp\build-testes.cmd` exit 0 e `tmp\build-app.cmd` exit 0.
